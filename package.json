{
  "name": "npm-scripts-example",
  "version": "1.0.0",
  "description": "npm scripts example",
  "scripts": {
    "clean": "rimraf ./dist && mkdir dist",
    "lint": "jshint src/**.js",
    "build-js": "babel src  --presets es2015 --watch --out-dir dist"
  },
  "dependencies": {
    "babel": "^6.5.2"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0"
  }
}
